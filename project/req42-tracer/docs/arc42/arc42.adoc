= project — Architecture (ARC42)

:description: REQ42 + ARC42 Project
:module: https://github.com/paul-fleischmann-com/req42-tracer

== 1. Introduction and Goals

=== 1.1 Requirements Overview

This section briefly lists the main quality requirements and project goals.

==== Quality Goals

- Learnability: Users productive within 30 minutes
- Reliability: Consistent traceability across all tool runs
- Maintainability: Clear separation of concerns

=== 1.2 Stakeholders

- Architecture Team
- Requirements Engineers
- QA/Test Teams
- ASPICE Auditors

== 2. Constraints

List technical, organizational, and regulatory constraints.

==== Technical Constraints

- Language: Go 1.24+
- CLI framework: Cobra
- Configuration: YAML (.req42.yaml)

==== Platform Compatibility

Supports multiple platforms and operating systems.

==== Organizational Constraints

- Single-page interactive reports
- No npm/Node.js dependencies for production

== 3. Context and Scope

Describes the system context and scope.

[arch,id=system.context,aspice=SWE.2,req=REQ-PARSE-001,REQ-PARSE-002,REQ-PARSE-003]
==== System Context Diagram

The project tool connects three primary data sources:

- AsciiDoc files (requirements, architecture, test specs)
- Bausteinsicht architecture model (JSON)
- CI test reports (JUnit XML, go-test JSON)

[arch,id=diag.context,parent=comp.system,aspice=SWE.2,impl=docs/arc42/diagrams/context.adoc,req=REQ-REPORT-001]
==== Context Diagram (Bausteinsicht)

Generated from `architecture.jsonc` via `scripts/generate-diagrams.sh`.

include::diagrams/context.adoc[]

== 4. Solution Strategy

=== 4.1 Overview

The tool follows a graph-based architecture:

1. Parse AsciiDoc blocks into structured model
2. Load Bausteinsicht architecture model
3. Build traceability graph (requirements → architecture → tests)
4. Analyze for gaps, coverage, ASPICE compliance
5. Generate reports (CLI, HTML)

[arch,id=strategy.graph,aspice=SWE.2,req=REQ-GRAPH-001,REQ-VALIDATE-001]
==== Traceability Graph

- Nodes: Requirement, Architecture Element, Test Spec, Test Code, Test Result
- Edges: satisfies, implements, verifies, derives
- Analysis: coverage, orphans, stale-detection

== 5. Building Block View

[arch,id=comp.system,aspice=SWE.2,req=SYS-TRACE-001,SYS-TOOL-001,SYS-QUAL-001]
=== req42-tracer System

Top-level system boundary containing the three containers: CLI, Backend, Frontend.

=== 5.0 Container Overview

[arch,id=diag.containers,parent=comp.system,aspice=SWE.2,impl=docs/arc42/diagrams/containers.adoc,req=REQ-REPORT-001]
==== Container Diagram (Bausteinsicht)

Generated from `architecture.jsonc` via `scripts/generate-diagrams.sh`.

include::diagrams/containers.adoc[]

=== 5.1 Level 1 — System Containers

[arch,id=container.cli,parent=comp.system,aspice=SWE.2,impl=cmd/req42-tracer,req=REQ-INIT-001,REQ-REPORT-001,REQ-REPORT-002,REQ-VALIDATE-001,REQ-WATCH-001]
==== CLI Container

Cobra-based CLI entry points. Each command delegates to the Backend for analysis.

[arch,id=container.backend,parent=comp.system,aspice=SWE.2,impl=internal,req=REQ-PARSE-001,REQ-PARSE-002,REQ-PARSE-003,REQ-GRAPH-001,REQ-ASPICE-001,REQ-TESTING-001,REQ-CONFIG-001,REQ-PLUGIN-001,REQ-PLUGIN-002,REQ-LSP-001]
==== Backend Container

Core analysis engine. All business logic resides here.

[arch,id=container.frontend,parent=comp.system,aspice=SWE.2,impl=internal/report,req=REQ-REPORT-001,REQ-REPORT-002]
==== Frontend Container

Interactive HTML report views rendered by the embedded D3.js + JavaScript application.
Generated by the Backend Report Generator; no server required after generation.

=== 5.2 Level 2 — CLI Components

[arch,id=diag.cli,parent=container.cli,aspice=SWE.2,impl=docs/arc42/diagrams/cli.adoc,req=REQ-REPORT-001]
==== Component Diagram — CLI (Bausteinsicht)

include::diagrams/cli.adoc[]

[arch,id=comp.init,parent=container.cli,aspice=SWE.2,impl=cmd/req42-tracer/init.go,req=REQ-INIT-001]
==== Initialization Command

Handles project initialization and setup commands.

[arch,id=comp.trace,parent=container.cli,aspice=SWE.2,impl=cmd/req42-tracer/trace.go,req=REQ-REPORT-001,REQ-VALIDATE-001]
==== Trace Command

Runs the traceability analysis and produces the traceability matrix report.

[arch,id=comp.gaps,parent=container.cli,aspice=SWE.2,impl=cmd/req42-tracer/gaps.go,req=REQ-REPORT-002,REQ-VALIDATE-001]
==== Gaps Command

Analyses the traceability graph and reports orphan elements and missing links.

[arch,id=comp.validate,parent=container.cli,aspice=SWE.2,impl=cmd/req42-tracer/validate.go,req=REQ-VALIDATE-001,REQ-CONFIG-001,REQ-VALIDATE-002]
==== Validate Command

Validates project structure, configuration, and cross-references without producing a full report.

[arch,id=comp.watch,parent=container.cli,aspice=SWE.2,impl=cmd/req42-tracer/watch.go,req=REQ-WATCH-001]
==== Watch Command

Monitors documentation files with fsnotify, regenerates the HTML report on change,
and serves it via HTTP with a polling live-reload script injected before `</body>`.

=== 5.3 Level 2 — Backend Components

[arch,id=diag.backend,parent=container.backend,aspice=SWE.2,impl=docs/arc42/diagrams/backend.adoc,req=REQ-REPORT-001]
==== Component Diagram — Backend (Bausteinsicht)

Generated from `architecture.jsonc` via `scripts/generate-diagrams.sh`.

include::diagrams/backend.adoc[]

[arch,id=diag.backend_level2,parent=container.backend,aspice=SWE.2,impl=docs/arc42/diagrams/backend_level2.adoc,req=REQ-REPORT-001]
==== Component Diagram — Backend Level 2 (Bausteinsicht)

include::diagrams/backend_level2.adoc[]

[arch,id=comp.parser,parent=container.backend,aspice=SWE.2,impl=internal/parser,req=REQ-PARSE-001,REQ-PARSE-002,REQ-PARSE-003,REQ-INIT-001,SWR-C-PARSER-001]
==== Parser Component

Reads AsciiDoc files, Bausteinsicht models and C/H source annotations; extracts all traceability blocks.

[arch,id=comp.graph,parent=container.backend,aspice=SWE.2,impl=internal/graph,req=REQ-GRAPH-001,REQ-REPORT-001,REQ-REPORT-002]
==== Graph Component

Builds and analyzes the traceability graph.

[arch,id=comp.aspice,parent=container.backend,aspice=SWE.2,impl=internal/aspice,req=REQ-ASPICE-001,REQ-TESTING-001,SYS-ASPICE-001]
==== ASPICE Checker

Validates process coverage against PAM 4.0.

[arch,id=comp.model,parent=container.backend,aspice=SWE.2,impl=internal/model,req=REQ-CONFIG-001,SWR-IMPL-REF-001]
==== Domain Model

Shared data types (`Requirement`, `ArchElement`, `TraceLink`, `Graph`) and configuration loading.

[arch,id=comp.testresult,parent=container.backend,aspice=SWE.2,impl=internal/testresult,req=REQ-TESTING-001,SWR-UNITY-PARSER-001,SWR-CUNIT-PARSER-001]
==== Test Result Loader

Loads test execution results from JUnit XML, go-test JSON, Unity and CUnit XML formats.

[arch,id=comp.templates,parent=container.backend,aspice=SWE.2,impl=internal/templates,req=REQ-INIT-001]
==== Project Templates

Embedded default templates (req42.adoc, arc42.adoc, architecture.jsonc, .req42.yaml) used by the init command.

[arch,id=comp.plugin,parent=container.backend,aspice=SWE.2,impl=internal/plugin/plugin.go,req=REQ-PLUGIN-001,REQ-PLUGIN-002,REQ-PLUGIN-003]
==== Plugin System

Provides stdin/stdout JSON protocol runner for external plugin binaries.
Supports 6 plugin categories: parser, enricher, validator, linker, reporter, notifier.

[test-spec,id=TS-PLUGIN-INTEGRATION-001,arch=comp.plugin,req=REQ-PLUGIN-001,aspice=SWE.5]
==== Integration Test: Plugin System stdin/stdout JSON protocol
Tests that the plugin runner correctly communicates with external binaries via stdin/stdout JSON.

[arch,id=comp.lsp,parent=container.backend,aspice=SWE.2,impl=internal/lsp,req=REQ-LSP-001]
==== LSP Server

Language Server Protocol backend (JSON-RPC 2.0 over stdio).
Provides completion, hover, diagnostics, and go-to-definition for IDE integration.

=== 5.4 Level 2 — Frontend Components

[arch,id=diag.frontend,parent=container.frontend,aspice=SWE.2,impl=docs/arc42/diagrams/frontend.adoc,req=REQ-REPORT-001]
==== Component Diagram — Frontend (Bausteinsicht)

include::diagrams/frontend.adoc[]

[arch,id=comp.report,parent=container.frontend,aspice=SWE.2,impl=internal/report,req=REQ-REPORT-001,REQ-REPORT-002]
==== Report Generator

Generates the interactive HTML report (Graph View, Matrix View, ASPICE Dashboard, Gaps Tab)
and CLI table reports. The HTML report is a self-contained single-page application.

=== 5.5 Level 3 — Parser DSN

[dsn,id=comp.parser.adoc,arch=comp.parser,aspice=SWE.3,impl=internal/parser/adoc.go,req=REQ-PARSE-001,REQ-INIT-001]
==== AsciiDoc Scanner

Extracts block attributes from .adoc files using regex.

[dsn,id=comp.parser.arch,arch=comp.parser,aspice=SWE.3,impl=internal/parser/adoc.go,req=REQ-PARSE-002]
==== Architecture Block Parser

Parses AsciiDoc [arch] blocks with hierarchy support and requirement linking.

[dsn,id=comp.parser.jsonc,arch=comp.parser,aspice=SWE.3,impl=internal/parser/bausteinsicht.go,req=REQ-PARSE-003]
==== Bausteinsicht Loader

Parses JSONC architecture model.

[dsn,id=comp.parser.gocode,arch=comp.parser,aspice=SWE.3,impl=internal/parser/gocode.go,req=REQ-TESTING-001]
==== Go Test-Code Parser

Parses Go test files and extracts `[test-spec]` annotations for explicit
TestResult→TestSpec linkage (see ADR-003).

[dsn,id=comp.parser.c,arch=comp.parser,aspice=SWE.3,impl=src/internal/parser/cparser.go,req=SWR-C-PARSER-001]
==== C-Quellcode-Parser

Walks configured `c-sources` directories, extracts `@req`, `@arch`, `@aspice` and
inline `req:`/`arch:` annotations from `.c`/`.h` files, and returns a map of
`archElementID → file:func:line` impl-ref patches applied via `Builder.ApplyImplRefs()`.

=== 5.6 Level 3 — Graph DSN

[dsn,id=comp.graph.builder,arch=comp.graph,aspice=SWE.3,impl=internal/graph/build.go,req=REQ-GRAPH-001]
==== Graph Builder

Constructs traceability graph from parsed elements.

[dsn,id=comp.graph.analyzer,arch=comp.graph,aspice=SWE.3,impl=internal/graph/analysis.go,req=REQ-REPORT-002,REQ-VALIDATE-001]
==== Graph Analyzer

Detects gaps, orphans, stale traces, and validates coverage.

=== 5.7 Level 3 — Frontend / Report DSN

[dsn,id=comp.report.graph,arch=comp.report,aspice=SWE.3,impl=internal/report/graph.go,req=REQ-REPORT-001]
==== Graph Data Exporter

Converts TraceabilityGraph to D3.js-compatible node/edge structure including
DesignElement nodes (type "dsn", group 4).

[dsn,id=comp.report.matrix,arch=comp.report,aspice=SWE.3,impl=internal/report/matrix.go,req=REQ-REPORT-001,REQ-REPORT-002]
==== Matrix Builder

Builds Requirement × ArchElement × DesignElement traceability matrix with
coverage statistics. Exports to CSV via `ExportMatrixToCSV`.

[dsn,id=comp.report.aspice,arch=comp.report,aspice=SWE.3,impl=internal/report/aspice_data.go,req=REQ-ASPICE-001]
==== ASPICE Dashboard Data

Computes ASPICE PAM 4.0 BP-coverage metrics for HTML dashboard rendering.

[dsn,id=comp.report.html,arch=comp.report,aspice=SWE.3,impl=internal/report/html.go,req=REQ-REPORT-001]
==== HTML Report Generator

Generates interactive HTML report (Graph View, Matrix View, ASPICE Dashboard,
Gaps Tab) by injecting JSON into the embedded D3.js template.

[dsn,id=comp.report.table,arch=comp.report,aspice=SWE.3,impl=internal/report/table.go,req=REQ-REPORT-001,REQ-REPORT-002]
==== CLI Table Reporter

Renders traceability matrix and gap analysis as ASCII/Markdown table output.

=== 5.8 Level 3 — TestResult DSN

[dsn,id=comp.testresult.gotest,arch=comp.testresult,aspice=SWE.3,impl=internal/testresult/gotest.go,req=REQ-TESTING-001]
==== Go-Test JSON Parser

Parses `go test -json` output format into TestResult objects.
ID format: `shortPackage::TestName` (e.g. `parser::TestBausteinsichtParser_Parse`).

[dsn,id=comp.testresult.junit,arch=comp.testresult,aspice=SWE.3,impl=internal/testresult/junit.go,req=REQ-TESTING-001]
==== JUnit XML Parser

Parses JUnit XML test result files. Supports Java dot-separated classnames
and Go slash-separated package paths via `shortPackage()`.

[dsn,id=comp.testresult.loader,arch=comp.testresult,aspice=SWE.3,impl=internal/testresult/loader.go,req=REQ-TESTING-001]
==== Test Result Loader

Dispatches to format-specific parsers via `Loader.Load()` and bulk-loads
all configured test sources via `LoadAll()`.

[dsn,id=comp.testresult.unity,arch=comp.testresult,aspice=SWE.3,impl=src/internal/testresult/unity.go,req=SWR-UNITY-PARSER-001]
==== Unity XML Parser

Parses Unity test-runner XML output format into TestResult objects.
Used for Embedded/AUTOSAR C unit tests. Format key: `unity`.

[dsn,id=comp.testresult.cunit,arch=comp.testresult,aspice=SWE.3,impl=src/internal/testresult/cunit.go,req=SWR-CUNIT-PARSER-001]
==== CUnit Automated XML Parser

Parses CUnit `CUnitAutomated.xml` output into TestResult objects.
Handles `<CUNIT_RUN_TEST_SUCCESS>` and `<CUNIT_RUN_TEST_FAILURE>` elements. Format key: `cunit`.

=== 5.9 Level 3 — ImplRef DSN

[dsn,id=comp.model.implref,arch=comp.model,aspice=SWE.3,impl=src/internal/model/impl.go,req=SWR-IMPL-REF-001]
==== ImplRef Model

`ParseImplRef(s)` parses all four `impl=` formats (Go package, file, file:func, file:func:line).
`ImplRef.ImplLink(baseURL)` generates a GitHub blob URL. Used by HTML report and validation rule.

=== 5.10 Level 3 — LSP DSN

[dsn,id=comp.lsp.complete,arch=comp.lsp,aspice=SWE.3,impl=internal/lsp/complete.go,req=REQ-LSP-001]
==== LSP Completion Provider

Detects `req=`, `arch=`, `test-spec=` attribute context from cursor position
and returns filtered, sorted CompletionList from the traceability graph.

[dsn,id=comp.lsp.hover,arch=comp.lsp,aspice=SWE.3,impl=internal/lsp/hover.go,req=REQ-LSP-001]
==== LSP Hover Provider

Detects `req=`, `arch=`, `test-spec=` value under cursor and returns Markdown
tooltip with title, description and metadata from the traceability graph.

[dsn,id=comp.lsp.diag,arch=comp.lsp,aspice=SWE.3,impl=internal/lsp/diag.go,req=REQ-LSP-001]
==== LSP Diagnostics Provider

Scans document lines for unknown `req=`, `arch=`, `test-spec=` values and
publishes `textDocument/publishDiagnostics` notifications with error-severity
ranges pointing to the unknown ID span.

[dsn,id=comp.lsp.godef,arch=comp.lsp,aspice=SWE.3,impl=internal/lsp/godef.go,req=REQ-LSP-001]
==== LSP Go-to-Definition Provider

Resolves `req=`, `arch=`, `test-spec=` value under cursor to a `Location`
(file URI + 0-based line) using `FilePath`/`LineNumber` from the graph.
Supports cross-file navigation to any parsed source file.

=== 5.11 Level 3 — Plugin DSN

[dsn,id=comp.plugin.runner,arch=comp.plugin,aspice=SWE.3,impl=internal/plugin/plugin.go,req=REQ-PLUGIN-001]
==== Plugin Runner

Core execution engine: starts subprocess via `exec.CommandContext`, writes JSON request
to stdin, reads JSON response from stdout, applies 30s timeout. Returns typed result
or error on non-zero exit code.

[dsn,id=comp.plugin.parsers,arch=comp.plugin,aspice=SWE.3,impl=internal/plugin/plugin.go,req=REQ-PLUGIN-001]
==== Parser Runners

Typed runners for each parser plugin type:

- `RunTestResultParser` → `[]*model.TestResult`
- `RunRequirementParser` → `[]*model.Requirement`
- `RunArchParser` → `[]*model.ArchElement`
- `RunTestSpecParser` → `[]*model.TestSpec`
- `RunDesignParser` → `[]*model.DesignElement`

[dsn,id=comp.plugin.enricher,arch=comp.plugin,aspice=SWE.3,impl=internal/plugin/plugin.go,req=REQ-PLUGIN-002]
==== Enricher Runner

Calls enricher plugin with graph snapshot, receives `[]EnrichmentPatch`,
applies patches to `Requirements`, `ArchElements`, `TestSpecs` via `ApplyEnrichments`.

[dsn,id=comp.plugin.validator,arch=comp.plugin,aspice=SWE.3,impl=internal/plugin/plugin.go,req=REQ-PLUGIN-002]
==== Validator Runner

Calls validator plugin with graph snapshot, returns `[]ValidationIssue`
(rule, severity, element_id, message). Issues are collected alongside built-in validation results.

[dsn,id=comp.plugin.linker,arch=comp.plugin,aspice=SWE.3,impl=internal/plugin/plugin.go,req=REQ-PLUGIN-002]
==== Linker Runner

Calls linker plugin with graph snapshot, returns `[]*model.TraceLink`.
Links are merged into the builder before `BuildLinks()` executes.

[dsn,id=comp.plugin.reporter,arch=comp.plugin,aspice=SWE.3,impl=internal/plugin/plugin.go,req=REQ-PLUGIN-002]
==== Reporter & Notifier Runner

- `RunReporter` — calls reporter plugin with graph + output_dir, returns generated file paths
- `RunNotifier` — calls notifier plugin with gaps payload as side-effect trigger

[test-spec,id=TS-PLUGIN-PARSERS-001,dsn=comp.plugin.parsers,req=REQ-PLUGIN-001,aspice=SWE.4-BP4]
==== Unit Test: Plugin Parser Runners execute typed stdin/stdout protocol

[test-spec,id=TS-PLUGIN-ENRICHER-001,dsn=comp.plugin.enricher,req=REQ-PLUGIN-002,aspice=SWE.4-BP4]
==== Unit Test: Plugin Enricher Runner applies patches to graph

[test-spec,id=TS-PLUGIN-VALIDATOR-001,dsn=comp.plugin.validator,req=REQ-PLUGIN-002,aspice=SWE.4-BP4]
==== Unit Test: Plugin Validator Runner returns validation issues

[test-spec,id=TS-PLUGIN-LINKER-001,dsn=comp.plugin.linker,req=REQ-PLUGIN-002,aspice=SWE.4-BP4]
==== Unit Test: Plugin Linker Runner merges trace links into graph

[test-spec,id=TS-PLUGIN-REPORTER-001,dsn=comp.plugin.reporter,req=REQ-PLUGIN-002,aspice=SWE.4-BP4]
==== Unit Test: Plugin Reporter Runner generates output files

== 6. Runtime View

Describes dynamic behavior during execution.

[arch,id=runtime.trace,parent=container.cli,aspice=SWE.2,impl=src/cmd/req42-tracer/graph_pipeline.go,req=REQ-REPORT-001,REQ-VALIDATE-001,SYS-TRACE-001]
==== Trace Command Sequence

[mermaid]
....
sequenceDiagram
    participant CLI as req42-tracer trace
    participant Cfg as model.LoadConfig
    participant Par as parser.ParseAllFromDir
    participant Bau as parser.BausteinsichtParser
    participant GC  as parser.ParseGoTestFiles
    participant TR  as testresult.LoadAll
    participant Grp as graph.NewBuilder
    participant Rpt as report.HTMLReporter

    CLI->>Cfg: LoadConfig(.req42.yaml)
    Cfg-->>CLI: *Config

    CLI->>Par: ParseAllFromDir(docs/requirements)
    Par-->>CLI: *TraceabilityGraph (reqs)

    CLI->>Par: ParseAllFromDir(docs/arc42)
    Par-->>CLI: *TraceabilityGraph (arch)

    CLI->>Bau: Parse(architecture.jsonc)
    Bau-->>CLI: *TraceabilityGraph (bausteinsicht)

    CLI->>GC: ParseGoTestFiles(src/)
    GC-->>CLI: *TraceabilityGraph (testcode)

    CLI->>TR: LoadAll(graph, config)
    TR-->>CLI: (test results merged)

    CLI->>Grp: MergeGraph(all graphs)
    Grp-->>CLI: *TraceabilityGraph (complete)

    CLI->>Rpt: GenerateReport()
    Rpt-->>CLI: report.html
....

Steps:

1. Load configuration (.req42.yaml)
2. Parse requirements (AsciiDoc)
3. Load architecture model (Bausteinsicht)
4. Parse Go test annotations
5. Load test results (JUnit XML / go-test JSON)
6. Build and merge traceability graph
7. Generate report

[arch,id=runtime.analyzegaps,parent=container.backend,aspice=SWE.2,impl=src/internal/graph/analysis.go,req=REQ-REPORT-002]
==== AnalyzeGaps() Algorithm

[mermaid]
....
flowchart TD
    A[AnalyzeGaps start] --> B[buildIndex\nbuild req→arch, arch→test maps]
    B --> C{For each\nRequirement}
    C --> D{Has arch\nlinks?}
    D -- No --> E[→ OrphanRequirements]
    D -- Yes --> C

    B --> F{For each\nArchElement\nwith parent=}
    F --> G{Has req=\nlinks?}
    G -- No --> H[→ OrphanArchElements]
    G -- Yes --> F
    F --> I{Has impl=?}
    I -- No --> F
    I -- Yes --> J{Has integration\ntest-spec?}
    J -- No --> K[→ UntestedArchElements]
    J -- Yes --> F

    B --> L{For each\nDesignElement}
    L --> M{Has arch=\nparent?}
    M -- No --> N[→ OrphanDesignElements]
    M -- Yes --> L
    L --> O{Has test-spec\nwith dsn=?}
    O -- No --> P[→ UntestedDesignElements]
    O -- Yes --> L

    B --> Q{For each\nTestSpec}
    Q --> R{Has req=\nor arch=?}
    R -- No --> S[→ OrphanTestSpecs]
    R -- Yes --> Q

    B --> T{For each\nTestResult}
    T --> U{LinkedSpec\nor LinkedCode?}
    U -- No --> V[→ UntracedTestResults]
    U -- Yes --> T

    E & H & K & N & P & S & V --> W[Return GapAnalysisResult]
....

[arch,id=runtime.watch,parent=container.cli,aspice=SWE.2,impl=src/cmd/req42-tracer/watch.go,req=REQ-WATCH-001]
==== Watch Mode Sequence

1. Start file watcher (fsnotify)
2. On file change: rebuild graph, regenerate HTML
3. Trigger live-reload in browser

== 7. Deployment View

[arch,id=deploy.cli,parent=container.cli,aspice=SWE.2,impl=src/cmd/req42-tracer,req=REQ-COMPAT-001,REQ-COMPAT-002,SYS-TOOL-001,SYS-QUAL-001]
==== CLI Deployment

- Single compiled binary (`req42-tracer`)
- Platform: Linux, macOS, Windows
- Installation: `go install` or pre-built releases

== 8. Crosscutting Concepts

=== 8.1 Configuration Management

Central configuration via `.req42.yaml`:

[arch,id=concept.config,req=REQ-CONFIG-001,REQ-INIT-001,SYS-CONFIG-001]
==== Configuration

- Project definitions (multi-project support)
- Bausteinsicht model path
- Test result paths
- Validation rules

=== 8.2 Error Handling

[arch,id=concept.errors,req=REQ-VALIDATE-001]
==== Error Strategy

- Fail-fast on validation errors
- Warnings for non-critical issues
- Detailed error messages with file/line references

=== 8.3 Extensibility

[arch,id=concept.plugins,req=REQ-PLUGIN-001,REQ-PLUGIN-002]
==== Plugin Points

External binaries communicate via stdin/stdout JSON (30s timeout, no CGo).
Plugin categories: `parser` (5 types), `enricher`, `validator`, `linker`, `reporter`, `notifier`.
Configuration: `.req42.yaml` → `plugins.*` or `test-results[format=plugin]`.
See `examples/plugins/` for ready-to-use starter scripts.

== 9. Architecture Decisions

Key ADRs are documented in `docs/arc42/ADRs/`.

- ADR-001: AsciiDoc blocks as primary source of truth
- ADR-002: Graph-based traceability model
- ADR-003: ASPICE mapping from ARC42 chapter hierarchy

== 10. Quality Requirements

[arch,id=quality.coverage,req=REQ-TESTING-001,SYS-TEST-001]
==== Test Coverage

- Parser: ≥90% unit test coverage
- Graph builder: ≥85% coverage
- Integration: ≥70% coverage

[arch,id=quality.performance,req=REQ-PERF-001]
==== Performance

- Trace generation: <1s for typical project (100 requirements)
- HTML report generation: <2s
- Watch mode reload: <500ms

[arch,id=quality.security,req=REQ-VALIDATE-001,SYS-VALID-001]
==== Security

- No external network calls
- All file I/O sandboxed to configured paths
- Input validation for all JSONC/YAML parsing

== 11. Technical Debt

None currently tracked. Technical debt is logged as GitHub Issues with the `technical-debt` label.

== 12. Glossary

[glossary]
Requirement::
  A functional or non-functional system requirement, tagged with `[req,...]` in AsciiDoc.

Architecture Element::
  A system component or sub-component, tagged with `[arch,...]` in AsciiDoc.

Test Specification::
  A specification of test scenarios, tagged with `[test-spec,...]` in AsciiDoc.

Test Result::
  The outcome of a test execution, parsed from JUnit XML or go-test JSON.

Traceability::
  The bidirectional links between requirements, architecture, and tests.

ASPICE::
  Automotive SPICE (Software Process Improvement and Capability dEtermination), version PAM 4.0.

Graph::
  The internal dependency graph connecting all traced artifacts.
