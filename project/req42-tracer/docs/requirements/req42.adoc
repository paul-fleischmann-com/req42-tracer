= req42-tracer — Requirements (REQ42)

:description: req42-tracer Requirements Tracing & ASPICE Compliance Tool
:module: github.com/paul-fleischmann-com/req42-tracer

== Overview

This document defines the functional and non-functional requirements for req42-tracer using AsciiDoc [req] blocks with structured metadata.
Each requirement is tagged with a unique ID, version, priority, status, and ASPICE classification.

== Functional Requirements

=== CLI & Project Initialization

[req,id=REQ-INIT-001,version=1,priority=high,aspice=SWE.1,
 derives=SYS-TOOL-001,
 status=approved,reviewed-by=paulefl,reviewed-date=2026-05-07]
== System shall initialize new REQ42+ARC42 projects

[test-spec,id=TS-INIT-001,req=REQ-INIT-001,aspice=SWE.4-BP3]
====
Test: req42-tracer init creates project structure with all template files
====

The system shall provide `req42-tracer init` command that creates a new project with:
- Interactive prompts for project name, module path, description, and optional directory
- Automated mode (--interactive=false) with explicit flags for CI/CD automation
- Pre-configured project templates: req42.adoc, arc42.adoc, architecture.jsonc, .req42.yaml, .gitignore
- Directory creation if not present (--dir flag support)

Acceptance Criteria:
- `req42-tracer init` in interactive mode prompts for all project details
- `req42-tracer init --dir=./my-project --name=MyProject --module=github.com/user/myproject --interactive=false` creates project non-interactively
- All template files are correctly substituted with project metadata
- docs/requirements, docs/arc42, reports directories are created

=== AsciiDoc Parser

[req,id=REQ-PARSE-001,version=1,priority=high,aspice=SWE.1,
 derives=SYS-TRACE-001,
 status=approved,reviewed-by=paulefl,reviewed-date=2026-05-07]
== System shall parse AsciiDoc [req] blocks with metadata

[test-spec,id=TS-PARSE-001,dsn=comp.parser.adoc,req=REQ-PARSE-001,aspice=SWE.4-BP3]
====
Test: ParseAllFromDir extracts all [req] blocks with correct metadata from AsciiDoc
====

The system shall extract all [req,...] blocks from AsciiDoc files with the following attributes:
- id (required): Unique requirement identifier (e.g., SWE-001)
- version (optional): Integer version number
- priority (optional): high, medium, low
- status (optional): draft, approved, deprecated
- aspice (optional): ASPICE process ID (SWE.1, SWE.2, etc.)
- reviewed-by (optional): Reviewer name
- reviewed-date (optional): Date in YYYY-MM-DD format
- derives (optional): Comma-separated list of parent requirement IDs

The parser shall handle multi-line block attribute syntax and extract the first == heading as the requirement title.

Acceptance Criteria:
- All example requirements from req42.adoc are parsed correctly
- Multi-line block attributes (spanning lines) are properly concatenated
- All optional attributes are preserved in the Requirement struct
- Missing 'id' attribute causes the requirement to be skipped with an error log

[req,id=REQ-PARSE-002,version=1,priority=high,aspice=SWE.1,
 derives=SYS-TRACE-001,
 status=approved,reviewed-by=paulefl,reviewed-date=2026-05-07]
== System shall parse AsciiDoc [arch] blocks with hierarchy support

[test-spec,id=TS-PARSE-002,dsn=comp.parser.arch,req=REQ-PARSE-002,aspice=SWE.4-BP3]
====
Test: ParseAllFromDir extracts all [arch] blocks with parent-child hierarchy
====

The system shall extract all [arch,...] blocks from AsciiDoc files with the following attributes:
- id (required): Unique architecture element identifier (e.g., comp.api)
- parent (optional): Parent element ID for hierarchical decomposition (indicates ARC42 level 2+)
- req (optional): Comma-separated list of requirement IDs this element satisfies
- aspice (optional): ASPICE process ID (auto-derived from hierarchy if not present)
- impl (optional): Implementation pointer (file path or code reference)
- test-spec (optional): Linked test specification ID

The parser shall derive ASPICE levels automatically: level 1 (no parent) → SWE.2; level 2+ (with parent) → SWE.3.

Acceptance Criteria:
- All [arch] blocks are extracted with correct attribute parsing
- Parent-child hierarchies are preserved
- ASPICE levels are auto-derived when not manually specified
- Architecture elements can reference requirements via 'req' attribute

[req,id=REQ-PARSE-003,version=1,priority=high,aspice=SWE.1,
 derives=SYS-TRACE-001,
 status=approved,reviewed-by=paulefl,reviewed-date=2026-05-07]
== System shall load Bausteinsicht architecture models with JSONC

[test-spec,id=TS-PARSE-003,dsn=comp.parser.jsonc,req=REQ-PARSE-003,aspice=SWE.4-BP3]
====
Test: BausteinsichtParser loads architecture.jsonc and flattens to dot-notation IDs
====

The system shall load and parse Bausteinsicht JSONC models (architecture.jsonc) with:
- Nested element hierarchy (arbitrary depth)
- Flattening to dot-notation IDs (e.g., system.backend.api)
- Removal of JSONC comments before JSON parsing
- Support for optional relationships and metadata

The loaded model is merged into the traceability graph and can be cross-referenced by requirement IDs.

Acceptance Criteria:
- architecture.jsonc is parsed without errors
- Nested hierarchies are flattened to dot-notation paths
- JSONC comments are properly stripped
- Architecture elements are added to the graph as ArchElements

=== Traceability Graph & Analysis

[req,id=REQ-GRAPH-001,version=1,priority=high,aspice=SWE.2,
 derives=SYS-TRACE-001,
 status=approved,reviewed-by=paulefl,reviewed-date=2026-05-07]
== System shall construct and analyze traceability graph

[test-spec,id=TS-GRAPH-001,dsn=comp.graph.builder,req=REQ-GRAPH-001,aspice=SWE.5-BP4]
====
Test: Builder constructs bidirectional traceability graph from requirements and architecture
====

The system shall build a traceability graph with:
- Nodes: Requirements, Architecture elements, Test specifications, Test results
- Edges: satisfies, implements, verifies, derives
- Link resolution: explicit annotations (primary) + name-based matching (fallback)
- Bidirectional trace: requirement → architecture → test → result

The graph builder shall merge parsed artifacts (AsciiDoc + Bausteinsicht) and detect/report circular dependencies and orphan artifacts.

Acceptance Criteria:
- Traceability graph is constructed from parsed requirements, architecture, and test specs
- Cross-references are resolved bidirectionally
- Orphan detection identifies unreferenced elements
- Circular dependency detection prevents invalid links

=== ASPICE Compliance Checking

[req,id=REQ-ASPICE-001,version=1,priority=high,aspice=SWE.2,
 derives=SYS-ASPICE-001,
 status=approved,reviewed-by=paulefl,reviewed-date=2026-05-07]
== System shall validate ASPICE PAM 4.0 compliance

[test-spec,id=TS-ASPICE-001,req=REQ-ASPICE-001,aspice=SWE.5-BP4]
====
Test: Checker.CheckCompliance calculates correct coverage percentages per ASPICE process
====

The system shall provide `req42-tracer aspice` command that:
- Validates configured ASPICE processes (SWE.1–SWE.6, SYS.1–SYS.3)
- Checks best practices: traceability, testability, consistency, bidirectionality
- Calculates coverage percentages per process
- Reports missing / partially satisfied best practices
- Derives ASPICE levels automatically from ARC42 hierarchy (level 1 → SWE.2, level 2+ → SWE.3)

Acceptance Criteria:
- `req42-tracer aspice` outputs coverage report for all configured processes
- BP4 (Traceability) check ensures all requirements have architecture mappings
- BP6 (Testability) check ensures all requirements have test specifications
- Coverage percentages are calculated correctly (N satisfied / N total)

=== Report Generation

[req,id=REQ-REPORT-001,version=1,priority=high,aspice=SWE.2,
 derives=SYS-TRACE-001,
 status=approved,reviewed-by=paulefl,reviewed-date=2026-05-07]
== System shall generate traceability reports in multiple formats

[test-spec,id=TS-REPORT-001,req=REQ-REPORT-001,aspice=SWE.5-BP4]
====
Test: TableReporter outputs valid text/markdown/JSON formats with all trace data
====

The system shall provide `req42-tracer trace` command with output formats:
- **text**: ASCII table with requirement→architecture→test columns
- **markdown**: Markdown table suitable for README/documentation
- **json**: Machine-readable JSON with full graph structure

All formats shall support filtering by project, ASPICE process, status, and version.

Acceptance Criteria:
- `req42-tracer trace --format=markdown` produces valid Markdown
- JSON format includes all nodes, edges, and metadata
- Text format is human-readable with aligned columns
- Gap analysis (orphans, missing implementations) is included in text output

[req,id=REQ-REPORT-002,version=1,priority=high,aspice=SWE.2,
 derives=SYS-TRACE-001,
 status=approved,reviewed-by=paulefl,reviewed-date=2026-05-07]
== System shall provide gap analysis reporting

[test-spec,id=TS-REPORT-002,req=REQ-REPORT-002,aspice=SWE.5-BP4]
====
Test: Analyzer.AnalyzeGaps identifies orphan requirements and missing implementations
====

The system shall provide `req42-tracer gaps` command that identifies:
- **orphans**: Architecture elements without requirement mappings
- **missing**: Requirements without architecture implementations
- **stale**: References to outdated requirement versions
- **untested**: Requirements without test specifications

Gap report shall show file path, line number, and remediation guidance.

Acceptance Criteria:
- Gap analysis identifies all missing trace links
- Each gap includes source location (file:line)
- Report prioritizes by severity (error/warning/info)

=== Validation & Configuration

[req,id=REQ-VALIDATE-001,version=1,priority=high,aspice=SWE.1,
 derives=SYS-VALID-001,
 status=approved,reviewed-by=paulefl,reviewed-date=2026-05-07]
== System shall validate project structure and references

[test-spec,id=TS-VALIDATE-001,req=REQ-VALIDATE-001,aspice=SWE.5-BP4]
====
Test: validate command reports syntax errors with file:line location
====

The system shall provide `req42-tracer validate` command that:
- Checks for syntax errors in AsciiDoc blocks
- Validates that all referenced IDs exist (req, arch, test-spec)
- Detects circular dependencies
- Verifies configuration file consistency
- Reports detailed error location (file:line)

Acceptance Criteria:
- All syntax errors in .adoc files are reported with line numbers
- Unknown ID references are flagged as validation errors
- Circular dependencies are detected and reported
- Validation succeeds when project is clean

[req,id=REQ-VALIDATE-002,version=1,priority=medium,aspice=SWE.1,
 derives=SYS-VALID-001,
 status=approved,reviewed-by=paulefl,reviewed-date=2026-06-02]
== System shall detect arch blocks incorrectly used for SWE.3 detailed design

[test-spec,id=TS-VALIDATE-002,req=REQ-VALIDATE-002,aspice=SWE.4-BP4]
====
Test: validate rule arch-used-for-swe3 flags [arch,aspice=SWE.3,parent=X] blocks
====

The system shall provide a validation rule `arch-used-for-swe3` that detects
`[arch]` blocks tagged `aspice=SWE.3` with a `parent=` reference to an `aspice=SWE.2`
architecture element. These blocks should be declared as `[dsn]` (detailed design) instead.

Acceptance Criteria:
- Rule `arch-used-for-swe3` is configurable in `.req42.yaml` (error/warning/off)
- Each mistyped block is reported with its ID and source location
- Rule fires only when the parent element exists and has `aspice=SWE.2`
- `[arch,aspice=SWE.3]` blocks without a SWE.2 parent are not flagged

[req,id=REQ-CONFIG-001,version=1,priority=high,aspice=SWE.1,
 derives=SYS-CONFIG-001,
 status=approved,reviewed-by=paulefl,reviewed-date=2026-05-07]
== System shall load and validate .req42.yaml configuration

[test-spec,id=TS-CONFIG-001,req=REQ-CONFIG-001,aspice=SWE.4-BP3]
====
Test: LoadConfig parses .req42.yaml correctly and applies defaults for missing keys
====

The system shall support project configuration via .req42.yaml containing:
- Project definitions (path, docs directory per project)
- Bausteinsicht model path
- Test result paths (JUnit XML, go-test JSON)
- Validation rules (severity levels: error, warning, off)

All CLI commands shall load and apply configuration from .req42.yaml or use sensible defaults.

Acceptance Criteria:
- .req42.yaml is correctly parsed from all commands
- Configuration paths are resolved relative to project root
- Missing configuration keys fall back to defaults
- Configuration validation reports errors with remediation

== Non-Functional Requirements

[req,id=REQ-PERF-001,version=1,priority=high,aspice=SWE.3,
 derives=SYS-QUAL-001,
 status=approved,reviewed-by=paulefl,reviewed-date=2026-05-07]
== System shall complete trace analysis in <2 seconds for typical projects

[test-spec,id=TS-PERF-001,req=REQ-PERF-001,aspice=SWE.5-BP4]
====
Test: Benchmark confirms trace analysis completes in <2s for 100+ requirements
====

The traceability analysis (parsing + graph construction + reporting) shall complete in under 2 seconds for projects with:
- 100+ requirements
- 50+ architecture elements
- 50+ test specifications

This includes file I/O, parsing, and report rendering.

Acceptance Criteria:
- Benchmark test confirms <2s for 100+ requirements
- No unnecessary file reads or allocations
- Graph construction is linear time complexity

[req,id=REQ-COMPAT-001,version=1,priority=high,aspice=SWE.3,
 derives=SYS-QUAL-001,
 status=approved,reviewed-by=paulefl,reviewed-date=2026-05-07]
== System shall be implemented in Go 1.24 or later

[test-spec,id=TS-COMPAT-001,req=REQ-COMPAT-001,aspice=SWE.5-BP4]
====
Test: go build succeeds and go.mod specifies go 1.24+
====

The implementation language is Go 1.24+ for security, performance, and cross-platform compatibility.
No Python, Node.js, or runtime dependencies are permitted.

Acceptance Criteria:
- go.mod specifies go 1.24 or later
- Binary builds cleanly with `go build`
- No external runtime dependencies required
- Vendored dependencies are tracked in go.mod

[req,id=REQ-COMPAT-002,version=1,priority=high,aspice=SWE.3,
 derives=SYS-QUAL-001,
 status=approved,reviewed-by=paulefl,reviewed-date=2026-05-07]
== System shall support Linux, macOS, and Windows platforms

[test-spec,id=TS-COMPAT-002,req=REQ-COMPAT-002,aspice=SWE.5-BP4]
====
Test: CI matrix confirms build and test pass on linux, macos, windows
====

The CLI shall build and run correctly on:
- Linux (amd64, arm64)
- macOS (Intel, Apple Silicon)
- Windows (amd64)

CI/CD shall test all three platforms. Binaries shall be released for all three.

Acceptance Criteria:
- CI/CD builds and tests on all three platforms
- GitHub Actions matrix covers linux, macos, windows
- Release binaries include all three platforms
- No platform-specific path separators in code (use filepath package)

[req,id=REQ-TESTING-001,version=1,priority=high,aspice=SWE.2,
 derives=SYS-TEST-001,
 status=approved,reviewed-by=paulefl,reviewed-date=2026-05-07]
== System shall achieve 80%+ code coverage on critical paths

[test-spec,id=TS-TESTING-001,req=REQ-TESTING-001,aspice=SWE.5-BP4]
====
Test: go test -cover confirms ≥80% coverage on parser/ and graph/ packages
====

Code coverage targets:
- parser/: ≥80% (adoc.go, bausteinsicht.go)
- graph/: ≥80% (build.go, analysis.go)
- aspice/: ≥75% (processes.go, checker.go)
- testresult/: ≥75% (junit.go, gotest.go)

Coverage is measured via `go test -cover` and enforced in CI.

Acceptance Criteria:
- `go test -cover ./...` reports ≥80% for critical packages
- CI fails if coverage drops below thresholds
- All edge cases (missing files, invalid syntax) are tested

== Derived Requirements

[req,id=REQ-WATCH-001,version=1,priority=medium,aspice=SWE.3,derives=REQ-REPORT-001,
 status=approved,reviewed-by=paulefl,reviewed-date=2026-05-07]
== System shall provide watch mode with file monitoring

[test-spec,id=TS-WATCH-001,req=REQ-WATCH-001,aspice=SWE.4-BP3]
====
Test: watch command detects file changes and re-runs analysis within 1s
====

Derived from REQ-REPORT-001. The system shall provide `req42-tracer watch` command that:
- Monitors .adoc, .jsonc, and configuration files for changes
- Automatically re-runs validation and report generation
- Supports --open flag to launch HTML report in browser
- Updates HTML report in-place without server restart

Acceptance Criteria:
- File changes trigger automatic re-analysis
- HTML report updates on file save
- --open flag launches browser automatically
- Watch mode exits cleanly on Ctrl+C

[req,id=REQ-LSP-001,version=1,priority=medium,aspice=SWE.3,
 derives=SYS-TOOL-001,
 status=draft,reviewed-by=paulefl,reviewed-date=2026-05-07]
== System shall provide LSP server for IDE integration

[test-spec,id=TS-LSP-001,req=REQ-LSP-001,aspice=SWE.4-BP3]
====
Test: LSP server responds to initialize and textDocument/completion requests
====

The system shall provide `req42-tracer lsp` command that starts a Language Server Protocol (LSP) server on stdio supporting:
- Autocomplete: `req=` → suggests all known requirement IDs
- Hover: Show requirement title and metadata on hover
- Diagnostics: Red underline for unknown requirement IDs
- Go-to-Definition: F12 on `req=SWE-001` → jump to definition

Acceptance Criteria:
- LSP server starts on `req42-tracer lsp`
- VS Code can connect and perform autocomplete
- Diagnostics are reported for unknown IDs
- Go-to-Definition works for valid requirement IDs

[req,id=REQ-PLUGIN-001,version=1,priority=medium,aspice=SWE.1,
 derives=REQ-TESTING-001,
 status=approved,reviewed-by=paulefl,reviewed-date=2026-06-01]
== System shall support external plugin binaries for parsing custom formats

[test-spec,id=TS-PLUGIN-001,req=REQ-PLUGIN-001,aspice=SWE.4-BP3]
====
Test: plugin runner invokes binary, sends JSON request on stdin, parses JSON response from stdout
====

The system shall provide a plugin interface that allows external binaries to extend parsing and analysis without modifying the core:

- Plugin communication via stdin/stdout JSON protocol — no CGo, no HTTP
- Plugin types: `test-result-parser`, `requirement-parser`, `arch-parser`, `test-spec-parser`, `design-parser`
- Plugin configured in `.req42.yaml` under `plugins.parsers` or `test-results[format=plugin]`
- Exit code != 0 treated as warning; plugin is skipped, execution continues
- Timeout of 30 seconds per plugin invocation

Acceptance Criteria:
- Plugin binary receives `{"file": "...", "project": "..."}` on stdin
- Plugin response is parsed and merged into the traceability graph
- Failed plugins produce a warning but do not abort the run

[req,id=REQ-PLUGIN-002,version=1,priority=medium,aspice=SWE.1,
 derives=REQ-PLUGIN-001,
 status=approved,reviewed-by=paulefl,reviewed-date=2026-06-01]
== System shall support enricher, validator, linker, reporter and notifier plugins

[test-spec,id=TS-PLUGIN-002,req=REQ-PLUGIN-002,aspice=SWE.4-BP3]
====
Test: enricher patches applied to graph elements; validator issues returned; linker links merged; reporter files generated; notifier called with gaps payload
====

Extends REQ-PLUGIN-001 to cover post-parsing plugin types:

- `enricher` — receives graph snapshot, returns `[]EnrichmentPatch` to annotate elements with external metadata
- `validator` — receives graph snapshot, returns `[]ValidationIssue` with rule, severity, element_id, message
- `linker` — receives graph snapshot, returns `[]TraceLink` to add custom trace connections
- `reporter` — receives graph snapshot + output_dir, writes files and returns their paths
- `notifier` — receives gaps/metrics payload as side-effect trigger (Slack, Teams, audit log)

Acceptance Criteria:
- Enricher patches applied to Requirements, ArchElements, TestSpecs
- Validator issues collected and included in validation output
- Linker links merged before BuildLinks() so they participate in traceability
- Reporter files listed in command output
- Notifier exit code != 0 produces warning only

[req,id=REQ-PLUGIN-003,version=1,priority=low,aspice=SWE.1,
 derives=REQ-PLUGIN-001,
 status=approved,reviewed-by=paulefl,reviewed-date=2026-06-01]
== System shall ship example plugins for common formats

[test-spec,id=TS-PLUGIN-003,req=REQ-PLUGIN-003,aspice=SWE.4-BP3]
====
Test: example plugins in examples/plugins/ are present and follow the documented protocol
====

The system shall provide ready-to-use example plugin scripts:

- `examples/plugins/markdown-req-parser.py` — parses `## REQ-*: Title` blocks from Markdown
- `examples/plugins/custom-validator.sh` — enforces REQ-* naming convention
- `examples/plugins/keyword-linker.py` — links test results to requirements by keyword match
- `examples/plugins/slack-notifier.sh` — posts gap summary to Slack webhook

Acceptance Criteria:
- Each example follows stdin/stdout JSON protocol
- Each example is executable and includes usage comment

[req,id=SWR-C-PARSER-001,version=1,priority=high,aspice=SWE.1,
 status=approved,reviewed-by=paulefl,reviewed-date=2026-06-02]
== System shall parse traceability markers from C/H source files

[test-spec,id=TS-C-PARSER-001,req=SWR-C-PARSER-001,aspice=SWE.4-BP4]
====
Test: @req, @arch, @aspice and inline req:/arch: markers in .c/.h files are extracted and applied as impl= refs to arch elements
====

The system shall scan C/H source directories (configured via `c-sources:` in `.req42.yaml`) for `@req`, `@arch`, `@aspice` comment annotations and automatically set `impl=<file>:<func>:<line>` on the matching arch elements in the traceability graph.

Supported syntax:

- JavaDoc-style: `@req`, `@arch`, `@aspice` tags in `/** ... */` blocks
- Inline style: `req:`, `arch:` in `/* ... */` single-line comments

Acceptance Criteria:

- `@req SWR-XXX` and `@arch comp.X` markers in `.c`/`.h` files are recognized
- The function name and line number following the comment are captured
- impl= is set on the matching `[arch]` element in the graph
- No annotation is created for plain comments without traceability tags

[req,id=SWR-UNITY-PARSER-001,version=1,priority=high,aspice=SWE.1,
 status=approved,reviewed-by=paulefl,reviewed-date=2026-06-02]
== System shall parse Unity XML test result files

[test-spec,id=TS-UNITY-PARSER-001,req=SWR-UNITY-PARSER-001,aspice=SWE.4-BP4]
====
Test: Unity XML files are parsed into TestResult objects with correct pass/fail status
====

The system shall support Unity's test-runner XML output format (format key: `unity` in `.req42.yaml`) in addition to JUnit and go-test-json.

Acceptance Criteria:

- Unity XML is parsed to `TestResult` objects with correct `Status` (passed/failed)
- Failed tests include the error message from `<Message>`
- Project and platform are set from the loader configuration

[req,id=SWR-CUNIT-PARSER-001,version=1,priority=high,aspice=SWE.1,
 status=approved,reviewed-by=paulefl,reviewed-date=2026-06-02]
== System shall parse CUnit Automated XML test result files

[test-spec,id=TS-CUNIT-PARSER-001,req=SWR-CUNIT-PARSER-001,aspice=SWE.4-BP4]
====
Test: CUnit XML files (CUnitAutomated.xml) are parsed into TestResult objects with correct pass/fail status
====

The system shall support CUnit Automated XML output format (format key: `cunit` in `.req42.yaml`).

Acceptance Criteria:

- Successes and failures are parsed from `<CUNIT_RUN_TEST_SUCCESS>` / `<CUNIT_RUN_TEST_FAILURE>`
- Suite name is used as package, test name as TestName
- Failed tests include the assertion condition as Error

[req,id=SWR-IMPL-REF-001,version=1,priority=high,aspice=SWE.1,
 status=approved,reviewed-by=paulefl,reviewed-date=2026-06-02]
== System shall support file:func:line format for impl= attributes

[test-spec,id=TS-IMPL-REF-001,req=SWR-IMPL-REF-001,aspice=SWE.4-BP4]
====
Test: impl= values in file:func:line format are parsed correctly and rendered as clickable links in HTML report
====

The `impl=` attribute on `[arch]` and `[dsn]` blocks shall support four formats: Go package path, file-only, file:function, file:function:line. When `source-base-url` is configured, file-based impl= values generate a clickable GitHub link in the HTML report.

Additionally, `req42-tracer validate` shall flag impl= references to non-existent files via the `impl-file-not-found` rule.

Acceptance Criteria:

- All four formats are parsed correctly by `ParseImplRef`
- `ImplLink(baseURL)` generates correct `file#LN` URL
- HTML report renders clickable links when `source-base-url` is set
- `impl-file-not-found` validation rule flags missing files

== Open Questions & Future Phases

[NOTE]
====
The following are deferred to Phase 3 (IDE Integration) and Phase 5 (Production Ready):
- LSP server implementation detail (REQ-LSP-001 is draft)
- HTML report with D3.js graph visualization (Phase 2)
- Advanced filtering and view composition (Phase 5)
- Custom validation rule definitions (Phase 5)
===
